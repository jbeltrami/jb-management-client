<table id="clients-table" class="table">
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Date of Birth</th>
      <th>E-mail</th>
      <th>Delete Client</th>
      <th>Update Client</th>
      <th>Add Service</th>
    </tr>
 </thead>

 <tbody id="clients-table-body">
   {{#each clients as |client|}}
   <tr>
     <td>{{client.first_name}}</td>
     <td>{{client.family_name}}</td>
     <td>{{client.born_on}}</td>
     <td>{{client.email}}</td>
     <td>
       <button data-id="{{client.id}}" class="btn btn-danger destroy-client">Delete Client</button>
     </td>
     <td>
       <button data-id="{{client.id}}" class="btn btn-warning update-client">Update Client</button>
     </td>
     <td>
       <button data-id="{{client.id}}" class="btn btn-success create-service">Add Service</button>
     </td>
   </tr>
   {{/each}}
 </tbody>

</table>

<button class="btn btn-default">Add client</button>
